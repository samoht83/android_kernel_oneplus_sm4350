name: Build Android Kernel with KernelSU v0.9.5

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Kernel version tag (optional)'
        required: false
        default: 'main'
      kernelsu_version:
        description: 'KernelSU version'
        required: false
        default: 'v0.9.5'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: 📥 Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
        ref: ${{ github.event.inputs.kernel_version }}

    - name: 🔧 Setup Build Environment
      run: |
        # Free up disk space
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo apt clean
        df -h
        
        # Update package lists
        sudo apt update

    - name: 🛠️ Install Build Dependencies
      run: |
        sudo apt install -y \
          build-essential bc bison flex libssl-dev ccache \
          libelf-dev libncurses-dev lzop python3 git wget unzip \
          device-tree-compiler cpio kmod zip \
          gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

    - name: 🧰 Setup LLVM/Clang Toolchain
      run: |
        # Use APT repository for more reliable installation
        wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
        sudo add-apt-repository "deb http://apt.llvm.org/focal/ llvm-toolchain-focal-18 main"
        sudo apt update
        sudo apt install -y clang-18 lldb-18 lld-18
        
        # Set up environment
        echo "CLANG_PATH=/usr/lib/llvm-18" >> $GITHUB_ENV
        echo "/usr/lib/llvm-18/bin" >> $GITHUB_PATH

    - name: 💉 Clone KernelSU
      run: |
        git clone --depth=1 -b ${{ github.event.inputs.kernelsu_version }} https://github.com/tiann/KernelSU
        echo "KERNELSU_PATH=$(pwd)/KernelSU" >> $GITHUB_ENV

    - name: 🔀 Integrate KernelSU
      run: |
        cd $GITHUB_WORKSPACE
        echo "Applying KernelSU patch..."
        if [ -f "KernelSU/kernel/setup.sh" ]; then
          # Use new integration method if available
          bash KernelSU/kernel/setup.sh
        else
          # Fallback to old method
          bash KernelSU/tools/merge_kernel.sh $(pwd)
        fi

    - name: ⚙️ Configure Kernel Build Environment
      run: |
        # Export build variables
        echo "ARCH=arm64" >> $GITHUB_ENV
        echo "SUBARCH=arm64" >> $GITHUB_ENV
        echo "CLANG_TRIPLE=aarch64-linux-gnu-" >> $GITHUB_ENV
        echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
        echo "CROSS_COMPILE_COMPAT=arm-linux-gnueabi-" >> $GITHUB_ENV
        echo "KBUILD_BUILD_USER=GitHub-Actions" >> $GITHUB_ENV
        echo "KBUILD_BUILD_HOST=GitHub" >> $GITHUB_ENV

    - name: 🔧 Generate Kernel Config
      run: |
        # Check if defconfig exists
        if [ ! -f "arch/arm64/configs/vendor/nordn200_defconfig" ]; then
          echo "❌ defconfig not found at expected path"
          echo "Available defconfigs:"
          find arch/arm64/configs -name "*defconfig" | head -10
          exit 1
        fi
        
        make O=out vendor/nordn200_defconfig
        
        # Enable KernelSU in config
        echo "CONFIG_KPROBES=y" >> out/.config
        echo "CONFIG_HAVE_KPROBES=y" >> out/.config
        echo "CONFIG_KPROBE_EVENTS=y" >> out/.config

    - name: 🧱 Build Kernel
      run: |
        make O=out \
          ARCH=$ARCH \
          SUBARCH=$SUBARCH \
          CLANG_TRIPLE=$CLANG_TRIPLE \
          CROSS_COMPILE=$CROSS_COMPILE \
          CROSS_COMPILE_COMPAT=$CROSS_COMPILE_COMPAT \
          CC=clang-18 \
          HOSTCC=clang-18 \
          HOSTCXX=clang++-18 \
          LD=ld.lld-18 \
          AR=llvm-ar-18 \
          NM=llvm-nm-18 \
          OBJCOPY=llvm-objcopy-18 \
          OBJDUMP=llvm-objdump-18 \
          READELF=llvm-readelf-18 \
          OBJSIZE=llvm-size-18 \
          STRIP=llvm-strip-18 \
          LLVM_AR=llvm-ar-18 \
          LLVM_DIS=llvm-dis-18 \
          LLVM_NM=llvm-nm-18 \
          LLVM=1 \
          -j$(nproc)

    - name: 📦 Prepare AnyKernel3 Package
      run: |
        # Clone AnyKernel3
        git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git anykernel
        
        # Check if kernel image was built successfully
        if [ ! -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
          echo "❌ Kernel build failed - Image.gz-dtb not found"
          ls -la out/arch/arm64/boot/
          exit 1
        fi
        
        # Copy kernel image
        cp out/arch/arm64/boot/Image.gz-dtb anykernel/
        
        # Create AnyKernel3 properties
        cd anykernel
        cat > anykernel.sh << 'EOF'
        # AnyKernel3 Ramdisk Mod Script
        # osm0sis @ xda-developers
        
        ## AnyKernel setup
        # begin properties
        properties() { '
        kernel.string=KernelSU NordN200 Kernel
        do.devicecheck=1
        do.modules=0
        do.systemless=1
        do.cleanup=1
        do.cleanuponabort=0
        device.name1=nord200
        device.name2=nordn200
        supported.versions=11-14
        supported.patchlevels=
        '; } # end properties
        
        # shell variables
        block=/dev/block/bootdevice/by-name/boot;
        is_slot_device=0;
        ramdisk_compression=auto;
        patch_vbmeta_flag=auto;
        
        ## AnyKernel methods (DO NOT CHANGE)
        # import patching functions/variables - see for reference
        . tools/ak3-core.sh;
        
        ## AnyKernel file attributes
        # set permissions/ownership for included ramdisk_files
        set_perm_recursive 0 0 755 644 $ramdisk/*;
        set_perm_recursive 0 0 750 750 $ramdisk/init* $ramdisk/sbin;
        
        ## AnyKernel install
        dump_boot;
        write_boot;
        ## end install
        EOF
        
        # Create flashable zip
        zip -r9 ../KernelSU-NordN200-$(date +%Y%m%d-%H%M).zip * -x .git README.md

    - name: 📊 Build Summary
      run: |
        echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
        echo "- **Kernel**: NordN200" >> $GITHUB_STEP_SUMMARY
        echo "- **KernelSU Version**: ${{ github.event.inputs.kernelsu_version }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Build Date**: $(date)" >> $GITHUB_STEP_SUMMARY
        echo "- **Clang Version**: $(clang-18 --version | head -1)" >> $GITHUB_STEP_SUMMARY
        
        if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
          echo "- **Kernel Size**: $(ls -lh out/arch/arm64/boot/Image.gz-dtb | awk '{print $5}')" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Build Status**: SUCCESS" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Build Status**: FAILED" >> $GITHUB_STEP_SUMMARY
        fi

    - name: 📤 Upload Kernel Package
      uses: actions/upload-artifact@v4
      with:
        name: KernelSU-NordN200-$(date +%Y%m%d)
        path: |
          KernelSU-NordN200-*.zip
          out/arch/arm64/boot/Image.gz-dtb
        retention-days: 30

    - name: 📤 Upload Build Logs (on failure)
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-$(date +%Y%m%d)
        path: |
          out/.config
          out/compile.log
        retention-days: 7