name: Build KernelSU-next Kernel and Repack Boot Image

on:
  workflow_dispatch:
    inputs:
      stock_boot_url:
        description: 'URL to the stock boot.img for repacking'
        required: true
        type: string
        default: 'https://raw.githubusercontent.com/samoht83/android_kernel_oneplus_sm4350/55c73263178dda755db0a217037d4e59c12c24aa/stock/original_boot.img'

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CLANG_CACHE_DIR: ${{ github.workspace }}/clang
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"

    steps:
    - name: Checkout kernel source
      uses: actions/checkout@v4
      with:
        repository: 'samoht83/android_kernel_oneplus_sm4350'
        ref: 'lineage-22.2'
        path: kernel

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libtinfo5 ccache curl git python3 unzip gcc-aarch64-linux-gnu jq abootimg
        curl -L -o mkbootimg https://github.com/danielguerra69/mkbootimg/releases/download/v3.10/mkbootimg
        chmod +x mkbootimg

    - name: Restore ccache
      uses: actions/cache@v4
      with:
        path: ${{ env.CCACHE_DIR }}
        key: ccache-${{ github.workflow }}
        restore-keys: |
          ccache-

    - name: Restore Clang cache
      uses: actions/cache@v4
      with:
        path: ${{ env.CLANG_CACHE_DIR }}
        key: clang-${{ env.CLANG_CACHE_DIR }}-v1

    - name: Download and install Clang (if missing)
      run: |
        CLANG_URL="https://github.com/llvm/llvm-project/releases/download/llvmorg-21.1.0-rc2/clang+llvm-21.1.0-rc2-armv7a-linux-gnueabihf.tar.gz"
        if [ ! -d "${{ env.CLANG_CACHE_DIR }}/bin" ]; then
          mkdir -p "${{ env.CLANG_CACHE_DIR }}"
          curl -L "$CLANG_URL" | tar -xz --strip-components=0 -C "${{ env.CLANG_CACHE_DIR }}"
        fi
        echo "${{ env.CLANG_CACHE_DIR }}/bin" >> $GITHUB_PATH
        clang --version

    - name: Download Android GCC 4.9 prebuilts
      run: |
        if [ ! -d "$HOME/aarch64-gcc/bin" ]; then
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 $HOME/aarch64-gcc
        fi
        echo "$HOME/aarch64-gcc/bin" >> $GITHUB_PATH

    - name: Setup KernelSU-next via official setup.sh
      run: |
        cd kernel
        curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -

    - name: Ensure CONFIG_KSU in defconfig
      run: |
        DEFCONFIG=$(find kernel/arch/arm64/configs -name "*holi*_defconfig" | head -n1)
        if [ -n "$DEFCONFIG" ]; then
          if ! grep -q "^CONFIG_KSU=y" "$DEFCONFIG"; then
            echo "CONFIG_KSU=y" >> "$DEFCONFIG"
            echo "Added CONFIG_KSU=y to $DEFCONFIG"
          else
            echo "CONFIG_KSU already present in $DEFCONFIG"
          fi
        else
          echo "holi defconfig not found; aborting"
          exit 1
        fi

    - name: Download stock boot.img
      run: |
        curl -L "${{ github.event.inputs.stock_boot_url }}" -o stock_boot.img

    - name: Unpack stock boot.img with abootimg
      run: |
        mkdir unpacked
        abootimg -x stock_boot.img
        mv boot.img-* unpacked/
        ls -la unpacked

    - name: Replace kernel in unpacked boot folder
      run: |
        cp kernel/out/arch/arm64/boot/Image.gz-dtb unpacked/zImage
        ls -la unpacked/

    - name: Repack boot.img with mkbootimg
      run: |
        ./mkbootimg \
          --kernel unpacked/zImage \
          --ramdisk unpacked/initrd.img \
          --cmdline "$(cat unpacked/bootimg.cmdline)" \
          --base 0x10000000 \
          --pagesize 4096 \
          -o boot_kernelsu_next.img

    - name: Upload KernelSU-next boot.img
      uses: actions/upload-artifact@v4
      with:
        name: boot_kernelsu_next.img
        path: boot_kernelsu_next.img

    - name: Show ccache stats
      run: |
        ccache --show-stats