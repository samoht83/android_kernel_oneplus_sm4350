name: Build KernelSU Kernel and Repack Boot Image

on:
  workflow_dispatch:
    inputs:
      kernelsu_version:
        description: 'KernelSU version tag (e.g., v0.9.5)'
        required: true
        default: 'v0.9.5'
        type: string
      stock_boot_url:
        description: 'URL to the stock boot.img for repacking'
        required: true
        default: 'https://github.com/.../boot.img'
        type: string

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"

    steps:
    - uses: actions/checkout@v4

    # 1. Install deps for Clang & binutils
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libtinfo5 ccache curl git python3 unzip gcc-aarch64-linux-gnu

    # 2. Cache ccache
    - name: Setup ccache cache
      uses: actions/cache@v4
      with:
        path: .ccache
        key: ${{ runner.os }}-ccache-${{ github.run_id }}
        restore-keys: |
          ${{ runner.os }}-ccache-

    # 3. Download Clang
    - name: Download Clang Toolchain
      run: |
        CLANG_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/android-14.0.0_r50/clang-r450784d.tar.gz"
        mkdir -p $HOME/clang
        curl -L "$CLANG_URL" | tar -xz -C $HOME/clang
        echo "$HOME/clang/bin" >> $GITHUB_PATH

    # 4. Download Android aarch64 GCC 4.9 binutils
    - name: Download Android GCC binutils
      run: |
        git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 $HOME/aarch64-gcc
        echo "$HOME/aarch64-gcc/bin" >> $GITHUB_PATH

    # 5. Build kernel
    - name: Build kernel
      run: |
        export ARCH=arm64
        export SUBARCH=arm64
        export CC=clang
        export CROSS_COMPILE=aarch64-linux-android-
        export CLANG_TRIPLE=aarch64-linux-gnu-
        export PATH=$HOME/clang/bin:$PATH
        export PATH=$HOME/aarch64-gcc/bin:$PATH
        export KBUILD_BUILD_USER=github
        export KBUILD_BUILD_HOST=actions
        ccache --zero-stats
        make O=out holi_defconfig
        make -j$(nproc) O=out

    # 6. Save ccache stats
    - name: Save ccache stats
      run: |
        ccache --show-stats